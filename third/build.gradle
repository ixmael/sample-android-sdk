plugins {
    id 'maven-publish'
}

configurations.maybeCreate("default")
artifacts.add("default", file('libs/pro-2.3.1.aar'))

publishing {
    publications {
        release(MavenPublication) {
            groupId = 'mx.irm.third'
            artifactId = 'third'
            version = '3.2.1'

            afterEvaluate {
                // from components.release
                println "after evaluate"
            }
        }
    }
}
